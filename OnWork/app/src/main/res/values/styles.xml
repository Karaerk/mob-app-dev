<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Defines the base style of views -->
    <style name="AppTheme.Layout">
        <item name="android:padding">@dimen/layout_space</item>
    </style>

    <style name="AppTheme.Widget" />

    <style name="AppTheme.Widget.CardView" />

    <style name="AppTheme.Widget.CardView.View" parent="Base.CardView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="cardCornerRadius">4dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="AppTheme.Widget.CardView.ItemView" parent="AppTheme.Widget.CardView.View">
        <item name="android:layout_marginTop">@dimen/view_space</item>
        <item name="android:layout_marginBottom">@dimen/view_space</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="AppTheme.Widget.CardView.ItemTitle">
        <item name="android:paddingStart">@dimen/small_space</item>
        <item name="android:paddingEnd">@dimen/small_space</item>
    </style>

    <style name="AppTheme.Widget.Layout" />

    <style name="AppTheme.Widget.Layout.Horizontal">
        <item name="android:paddingStart">@dimen/layout_space</item>
        <item name="android:paddingEnd">@dimen/layout_space</item>
    </style>
    <style name="AppTheme.Widget.Layout.Vertical">
        <item name="android:paddingTop">@dimen/layout_space</item>
        <item name="android:paddingBottom">@dimen/layout_space</item>
    </style>

</resources>
